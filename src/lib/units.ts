// Shared unit-conversion constants — kept in one place since weight, goal
// weight, and height all convert on a unit switch, and BMI needs both.
export const LBS_PER_KG = 2.2046226218;
export const CM_PER_INCH = 2.54;
